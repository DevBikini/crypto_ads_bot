/*CMD
  command: /broadc
  help: 
  need_reply: 
  auto_retry_time: 
  folder: Broadcast
  answer: 
  keyboard: 
  aliases: 
CMD*/

var data = options.data
var number = 1
var msg =
  "✅ *New task available*\n\n❓ We found " +
  number +
  " *new promotions* available for you today!\n\nUse /notification to disable this notification"
Bot.sendMessageToChatWithId(data.split(" ")[no1], msg)
Bot.sendMessageToChatWithId(data.split(" ")[no2], msg)
Bot.sendMessageToChatWithId(data.split(" ")[no3], msg)
Bot.sendMessageToChatWithId(data.split(" ")[no4], msg)
Bot.sendMessageToChatWithId(data.split(" ")[no5], msg)
var data = options.data
var number = 1
var msg =
  "✅ *New task available*\n\n❓ We found " +
  number +
  " *new promotions* available for you today!\n\nUse /notification to disable this notification"
Bot.sendMessageToChatWithId(data.split(" ")[no1], msg)
Bot.sendMessageToChatWithId(data.split(" ")[no2], msg)
Bot.sendMessageToChatWithId(data.split(" ")[no3], msg)
Bot.sendMessageToChatWithId(data.split(" ")[no4], msg)
Bot.sendMessageToChatWithId(data.split(" ")[no5], msg)
